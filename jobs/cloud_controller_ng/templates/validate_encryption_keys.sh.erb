#!/usr/bin/env bash

function validate_encryption_keys {
    set +e

    bundle exec rake db:validate_encryption_keys
    result=$?
    if [[ $result -ne 0 ]]; then
      # Rake task will output any error messages
      exit $result
    fi

    set -e
}

function main {
  validate_encryption_keys
}

main

exit 0
